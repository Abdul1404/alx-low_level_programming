#include "main.h"

/**
 * print_line - prints an underscore _ n mumber of times
 *
 * @n: number of times to print
 *
 * Return: void
 *
 */
void print_line(int n)
{
	int j;

	if (!(n <= 0))
	{
		for (j = 0; j < n; j++)
		{
			_putchar('_');
		}
	}
	_putchar('\n');
}
